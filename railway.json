{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildEnvironment": "V3"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "startCommand": "sh -c \"exec php artisan serve --host=0.0.0.0 --port=${PORT:-8000}\"",
    "preDeployCommand": [
      "sh -c \"php artisan migrate --force && php artisan storage:link --force\""
    ],
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300,
    "sleepApplication": false,
    "useLegacyStacker": false,
    "ipv6EgressEnabled": true,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
